// create a config object to hold info about our events
// the idea is the same as in Redux, to hold the action names in variables an pass those around
// to avoid typo mistakes when referencing to the object 
module.exports = {
    events: {
        GREET: 'greet'
    }
};